# Binaries
/bin/
/dist/

# Frontend build output (embedded into Go binary at build time)
/web/dist/
/web/node_modules/

# Go
*.test
*.out
go.work
go.work.sum

# Frontend build artifacts
web/static/dist/
web/node_modules/

# Reference clone (kept locally for study, not committed)
/reference/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local env (real secrets — see .env.example for the template)
.env
.env.local
.env.production
